import 'package:flutter/material.dart';

class CreateCardboard extends StatelessWidget {
  const CreateCardboard({super.key});

  @override
  Widget build(Object context) {
    return Text("create_cardboard");
  }

}